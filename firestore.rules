rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 自分の原稿だけ読み書きできる。他人の uid の下には一切触れない。
    match /users/{uid}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
